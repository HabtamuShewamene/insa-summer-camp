# Personal CLI Toolbox

Small command-line tool for storing personal notes.

## Run

```bash
python notes.py add "Buy milk"
python notes.py list
python notes.py delete 1
```

## Notes

- Notes are stored in `notes.json` in the same folder.
- Each command is handled by its own function.
